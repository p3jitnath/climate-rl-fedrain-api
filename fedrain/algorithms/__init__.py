"""Algorithm implementations bundled with the FedRAIN facade.

This subpackage contains concrete RL algorithm implementations used by the
project (for example, DDPG) and is intentionally lightweight.
"""
